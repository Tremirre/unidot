--- a/Menu.qml
+++ b/Menu.qml
@@ -76,0 +77,4 @@
+  property int calculatorRevision: 0
+  property string calculatorResult: ""
+  property string calculatorError: ""
+  readonly property bool calculatorActive: !dmenuActive && filterText.trim().charAt(0) === "="
@@ -151,0 +156,18 @@
+
+  function updateCalculator() {
+    calculatorRevision += 1
+    calculatorError = ""
+    calculatorTimer.stop()
+
+    if (!root.calculatorActive) return
+
+    var expression = root.filterText.trim().slice(1).trim()
+    if (!expression) {
+      root.calculatorError = "Type an expression after ="
+      return
+    }
+
+    calculatorTimer.expression = expression
+    calculatorTimer.revision = calculatorRevision
+    calculatorTimer.restart()
+  }
@@ -585,1 +607,20 @@
-    if (query) {
+    if (root.calculatorActive) {
+      var calculatorLabel = root.calculatorResult || root.calculatorError
+      rows.push({
+        itemId: "calculator.result",
+        kind: "calculation",
+        icon: "󰃬",
+        iconFont: "",
+        appIcon: "",
+        appId: "",
+        label: calculatorLabel,
+        target: "",
+        detail: root.calculatorResult ? "Press Enter to copy" : "",
+        path: "",
+        childCount: 0,
+        action: "",
+        provider: "",
+        score: 0,
+        section: ""
+      })
+    } else if (query) {
@@ -687,0 +729,1 @@
+    root.updateCalculator()
@@ -740,0 +783,6 @@
+    if (row.kind === "calculation") {
+      if (!root.calculatorResult) return
+      Quickshell.execDetached(["bash", "-c", "printf %s " + Util.shellQuote(root.calculatorResult) + " | wl-copy"])
+      root.cancel()
+      return
+    }
@@ -887,0 +936,33 @@
+  Timer {
+    id: calculatorTimer
+    interval: 150
+    repeat: false
+    property string expression: ""
+    property int revision: 0
+    onTriggered: {
+      if (calculatorProc.running) calculatorProc.running = false
+      calculatorProc.command = ["qalc", "-t", "--", expression]
+      calculatorProc.revision = revision
+      calculatorProc.running = true
+    }
+  }
+
+  Process {
+    id: calculatorProc
+    property int revision: 0
+    stdout: StdioCollector { id: calculatorStdout; waitForEnd: true }
+    onExited: function(exitCode) {
+      if (revision !== root.calculatorRevision) return
+
+      var result = String(calculatorStdout.text || "").trim()
+      if (exitCode === 0 && result) {
+        root.calculatorResult = result
+        root.calculatorError = ""
+      } else {
+        root.calculatorResult = ""
+        root.calculatorError = "Invalid expression"
+      }
+      root.rebuildDisplay()
+    }
+  }
+
