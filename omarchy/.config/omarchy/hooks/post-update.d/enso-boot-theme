#!/bin/bash
set -euo pipefail

hook_path="$(readlink -f "${BASH_SOURCE[0]}")"
repo_dir="$(git -C "$(dirname "$hook_path")" rev-parse --show-toplevel)"

"$repo_dir/scripts/apply-omarchy-boot-theme.sh"
